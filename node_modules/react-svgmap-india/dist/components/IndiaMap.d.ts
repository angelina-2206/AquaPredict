import React from 'react';
import { MapPropType } from '../types/Types';
declare const IndiaMap: ({ className, size, mapColor, strokeColor, strokeWidth, hoverColor, onClick, }: MapPropType) => React.JSX.Element;
export default IndiaMap;
